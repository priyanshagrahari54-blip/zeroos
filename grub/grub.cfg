set timeout=0
set default=0
# Stage 5: force a linear graphics mode for the Multiboot2 framebuffer tag
# so the kernel display primitive acquires scanout on hardware/QEMU boots.
set gfxpayload=1024x768x32

menuentry "ZEROOS" {
    multiboot2 /boot/zeroos.elf
    boot
}
